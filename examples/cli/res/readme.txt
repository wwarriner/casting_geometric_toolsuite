Command Line Interface for CGT.

Use by calling from terminal with only a single argument. The argument should be
the absolute path to a settings.json file. See the included example
"cli_settings.json". Note that the input_stl and output values must be absolute
paths.

Usage:

> CGT.exe "absolute/path/to/settings.json"